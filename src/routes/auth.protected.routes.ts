import { Router } from "express";

const router = Router();

// Protected auth routes (examples):
// router.post("/logout", auth, logout);
// router.post("/change-password", auth, changePassword);
// Add more protected auth routes here

export default router;
